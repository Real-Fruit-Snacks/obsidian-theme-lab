## What changed

## Why

## Tested

- [ ] `node --check main.js` passes
- [ ] Reloaded the plugin in a vault and exercised the affected command
- [ ] Only the public Obsidian API is used; everything new is registered with `this.register*`
- [ ] `manifest.json` and `versions.json` are untouched (bumped at release time)
